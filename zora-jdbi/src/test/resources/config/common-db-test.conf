# these are our own config values defined by the app
flyway {
  enabled=true
}

# the database config
database {
  driver="org.postgresql.Driver"
  url="jdbc:postgresql://10.10.10.5:5432/peanotes"
  user="app_user"
  password=1
  auto_commit=true
}
